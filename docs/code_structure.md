# Code Structure

Last updated: 2026-05-25.

This file mirrors the short top-level `code_structure.md`. It exists so older
links into `docs/code_structure.md` still land on current guidance.

Use:

- `../code_structure.md` for the concise module map.
- `runtime_source_of_truth.md` for the scheduler and automatic/manual status.
- `agent_guidance.md` for coding and non-coding agent rules.

