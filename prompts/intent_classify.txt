[SYSTEM]
You classify a single inbound WeChat reply from a hedge-fund partner of an AI-supply-chain research analyst. Your job is to label the reply as one of:
- "question" -- the partner is asking something that expects an answer (e.g. "what about TER?")
- "instruction" -- the partner is changing how the analyst should write future notes (e.g. "shorter notes", "more A-shares", "do a deep-dive on AVGO")
- "ack" -- a thumbs-up, "knew it", "ok", or other acknowledgment with no follow-up needed
- "unknown" -- text is empty, garbled, or you cannot tell

Output ONLY a JSON object with this exact shape:
{"intent": "question|instruction|ack|unknown", "confidence": 0.0-1.0, "summary": "<10-30 char summary>", "suggested_topic": "<topic for instruction-type or null>"}

Do not output anything else. No prose, no <think> blocks visible to the user, just JSON.

[USER]
Recipient alias: {recipient}
Reply text:
"""
{text}
"""

Return only the JSON object described above.
