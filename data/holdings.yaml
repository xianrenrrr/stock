# Tracked portfolio holdings.
#
# SOURCE OF TRUTH = LIVE ROBINHOOD (since 2026-06-02). The `broker_positions_pull`
# orchestrator job (Mon-Fri every 30 min) pulls live positions via the codex/RH
# MCP and imports them into the `holdings` table; `broker_snapshot_import`
# deactivates positions you have exited. Holdings are NO LONGER managed here.
#
# This list is intentionally EMPTY so startup `sync_from_yaml()` is a no-op and
# does NOT fight the live pull (a populated list here would re-add stale names and
# cull live-only positions on every restart). To force-track a name that is NOT in
# Robinhood, add it via `stock holding add <TICKER> <qty> <cost>` instead of here.
holdings: []

# ---------------------------------------------------------------------------
# REFERENCE -- SMCI sell/take-profit triggers (kept for trading discipline).
# SMCI is currently held again per the live Robinhood pull; these triggers from
# the prior position still apply as a checklist:
#
#       === SELL TRIGGERS (any one = re-evaluate; multiple = exit) ===
#       1. Q3 print: margin stuck < 11% -> thesis broken, cut.
#       2. Hyperscaler capex guidance NOT upgraded by MSFT/META/GOOGL/AMZN
#          -> integration layer pre-priced; trim half.
#       3. GB200 NVL72 shipment cadence delay -> integration thesis pushes out -> exit.
#       4. Auditor / 10-K compliance event reignites -> unconditional exit.
#       5. DELL liquid-cooling rack price war disclosed -> trim 50%.
#       6. Single-day pct_change <= -8% on volume_ratio >= 2.0 -> stop-loss check.
#
#       === TAKE-PROFIT TRIGGERS ===
#       7. +30% from cost_basis with no new catalyst -> trim 1/3.
#       8. META or xAI names SMCI as DLC vendor of record -> trim 1/2 + raise stop to entry.
#
#       RECOMMENDED MAX POSITION SIZE: 5% of portfolio.
